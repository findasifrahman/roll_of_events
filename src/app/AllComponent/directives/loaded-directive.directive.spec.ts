import { LoadedDirectiveDirective } from './loaded-directive.directive';

describe('LoadedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
